1. It would be worth to rename the type '$iu' (integer unsigned) to the type '$p' (pointer).

2. Current definition of the terminal 'interpolatedString' may be rewritten to the following one in order to reduce the number of nodes in the AST:

interpolatedString
    : '"' text nonEmptyInterpolatedExprTextList '"'
    | '"' text '"'
    | '"' nonEmptyInterpolatedExprTextList '"'
    | '"' '"'
    : '"""' text nonEmptyInterpolatedExprTextList '"""'
    | '"""' text '"""'
    | '"""' nonEmptyInterpolatedExprTextList '"""'
    | '"""' '"""'
    ;

3. We could apply the function 'isTypeVoid' wider than we are applying it right now.
4. In the function 'src/translator/index.js:checkCallByName', we should use a better implementation for deciding which function to call. Implementation of the case '$size' should be used as an example.
5. Should we print the path of every module and the corresponding WebAssembly text (WAT) in the generated output file?
6. Review the definition of the concept "literal expression".
